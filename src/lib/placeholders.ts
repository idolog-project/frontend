/**
 * What stands in when a filming location's photograph will not load.
 *
 * One constant rather than the path repeated at each `<img>`: the pins, the
 * cards and anything that follows have to agree on it, and a string typed three
 * times is a string that will eventually differ in one of them.
 *
 * The file itself is the app's viewfinder mark — see the comment inside it for
 * why it is drawn the way it is.
 */
export const LOCATION_PLACEHOLDER = '/images/location-placeholder.svg'
